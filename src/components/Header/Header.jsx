import './Header.css';

const Header = () => {
  return (
    <header>
      <nav>
        <a href="#home">Home</a>
        <a href="#contacts">Contacts</a>
        <a href="#info">Info</a>
      </nav>
    </header>
  );
};

export default Header;